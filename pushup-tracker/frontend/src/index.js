import React from 'react';
import ReactDOM from 'react-dom';

import PushupTracker from "./component/PushupTracker.js"

import "./index.css"

ReactDOM.render(
  <PushupTracker />,
  document.getElementById('root')
);
